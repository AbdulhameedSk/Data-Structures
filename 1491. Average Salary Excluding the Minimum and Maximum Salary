import java.util.*;

class Solution {
    public double average(int[] salary) {
        Arrays.sort(salary);
        int l = salary.length;
        double sum = 0;
        for (int i = 1; i < l - 1; i++) {
            sum += salary[i];
        }
        return sum / (l - 2);
    }
}
